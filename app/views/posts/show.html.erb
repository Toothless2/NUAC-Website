<article>
  <h2><%= @post.title %></h2>

  <p>
    <%= sanitize markdown_body(@post.body) %>
  </p>

  <% if admin_user? %>
    <%= link_to 'Edit', edit_post_path(@post) %> |
  <%end%>
  <%= link_to 'Back', posts_path %>
</article>
