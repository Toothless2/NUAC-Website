<%= flash[:recaptcha_error] %>
<%= simple_form_for(@record) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :score %>
    <%= f.input :round, required: false, collection: Record.rounds, value_method: :first, label_method: :first, selected: 0 %>
    <%= f.input :bowstyle, required: false, collection: Record.bowstyles, value_method: :first, label_method: :first, selected: 0 %>
    <%= f.input :gender, required: false, collection: Record.genders, value_method: :first, label_method: :first, default: 0 %>
    <%= f.input :achived_at, required: false, as: :string, input_html: {type: :date, value: Date.today} %>
    <%= recaptcha_tags %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>